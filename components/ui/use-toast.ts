"use client";

// Compatibility re-export — the app's toasts live in hooks/use-toast (backed
// by react-hot-toast with the green/orange/red colour scheme).
export { useToast, toast } from "@/hooks/use-toast";
